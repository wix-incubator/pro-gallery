{
  "name": "pro-gallery-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "postinstall": "lerna bootstrap --hoist",
    "publish": "node scripts/publishPackages",
    "deploy": "node scripts/deployToSurge",
    "start": "npm run watch",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "watch": "npm run link-packages && lerna run --parallel watch",
    "link-packages": "cd packages/layouts && yarn link && cd ../lib && yarn link && yarn link pro-layouts && cd ../gallery && yarn link && yarn link pro-layouts && yarn link pro-gallery-lib && cd ../playground && yarn link pro-gallery && yarn link pro-gallery-lib && cd ../ssr-simulator && yarn link pro-gallery && cd ../..",
    "build-packages-by-order": "cd packages/layouts && npm run build && cd ../lib && npm run build && cd ../gallery && npm run build && cd ../playground && npm run build && cd ../..",
    "start-playground": "cd packages/playground && npm run start",
    "build-all-and-start-playground": "npm run link-packages && npm run build-packages-by-order && npm run start-playground",
    "build:dev": "npm run link-packages && npm run build-packages-by-order",
    "test:e2e": "start-server-and-test build-all-and-start-playground http-get://localhost:3000 'lerna run --parallel e2e-test'",
    "test": "npm run link-packages && npm run build-packages-by-order && lerna run --parallel test",
    "start-playground:ci": "cd packages/playground && npx serve -s build -l 3000 && cd ../..",
    "test:ci": "npm run link-packages && lerna run --parallel test",
    "test:e2e:layouts:ci": "npm run link-packages && start-server-and-test start-playground:ci http-get://localhost:3000 'lerna run e2e-layouts'",
    "test:e2e:styleParams:ci": "npm run link-packages && start-server-and-test start-playground:ci http-get://localhost:3000 'lerna run e2e-styleParams'",
    "test:all": "npm run test && npm run test:e2e",
    "patch": "npm run lint && npm run test:all && node scripts/releaseVersion --patch",
    "minor": "npm run lint && npm run test:all && node scripts/releaseVersion --minor",
    "major": "npm run lint && npm run test:all && node scripts/releaseVersion --major"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wix/pro-gallery.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/wix/pro-gallery/issues"
  },
  "homepage": "https://github.com/wix/pro-gallery#readme",
  "devDependencies": {
    "chalk": "^2.4.2",
    "gh-pages": "2.0.1",
    "lerna": "^3.13.3",
    "lerna-packages": "0.0.1",
    "lodash": "^4.17.4",
    "prettier": "^2.1.2",
    "prompt": "^1.0.0",
    "semver": "^6.3.0"
  },
  "dependencies": {
    "start-server-and-test": "^1.10.6"
  }
}
