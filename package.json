{
  "name": "pro-gallery-monorepo",
  "version": "5.0.143",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/blueprints",
    "packages/gallery",
    "packages/layouts",
    "packages/lib"
  ],
  "packageManager": "yarn@4.0.0",
  "scripts": {
    "lint": "yarn workspaces foreach --all run lint",
    "lint:fix": "yarn workspaces foreach --all run lint:fix",
    "build": "yarn workspaces foreach --all run build",
    "changelog": "sh scripts/changelog-builder.sh > CHANGELOG.md",
    "start-playground": "yarn workspace playground start",
    "test:e2e": "start-server-and-test build-all-and-start-playground http-get://localhost:3006 'yarn workspaces foreach --all run test:e2e-all'",
    "test": "echo 'not running tests from root - running it using GitHub actions'",
    "ci:run-playground": "yarn workspace playground exec serve -s build -l 3006",
    "ci:test-unit": "yarn workspaces foreach --all run unit",
    "ci:test-e2e-layouts": "start-server-and-test ci:run-playground http-get://localhost:3006 'yarn workspaces foreach --all run test:e2e-layouts'",
    "ci:test-e2e-styleParams": "start-server-and-test ci:run-playground http-get://localhost:3006 'yarn workspaces foreach --all run test:e2e-styleParams'",
    "lint-staged": "lint-staged",
    "ci:test-e2e-integration": "start-server-and-test ci:run-playground http-get://localhost:3006 'yarn workspaces foreach --all run test:e2e-integration'",
    "build-all-and-start-playground": "yarn start-playground"
  },
  "overrides": {
    "cheerio": "1.0.0-rc.3"
  },
  "lint-staged": {
    "packages/**/*.{js,jsx,ts,tsx}": "yarn workspaces foreach --all run lint:fix"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wix/pro-gallery.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/wix/pro-gallery/issues"
  },
  "homepage": "https://github.com/wix/pro-gallery#readme",
  "devDependencies": {
    "chalk": "^2.4.2",
    "gh-pages": "2.0.1",
    "husky": "^4.3.0",
    "lint-staged": "^10.5.2",
    "lodash": "^4.17.21",
    "prettier": "^2.1.2",
    "prompt": "^1.0.0",
    "semver": "^6.3.0",
    "set-value": "^4.1.0"
  },
  "dependencies": {
    "start-server-and-test": "^1.10.6"
  }
}
