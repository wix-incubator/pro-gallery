export default class PlaygroundsBlueprintsApi {

  getMoreItems() {
    console.log('Playgrounds BluepritnsApi >>> getMoreItems called', ' Doing nothing event will propagate for added items props');
    const eventHandledInternaly = false;
    const items = null;
    return {eventHandledInternaly, items};
  }
}