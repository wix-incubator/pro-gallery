import { INPUT_TYPES } from '../utils/constants';

export default {
  title: 'Gallery Spacing',
  description: `Set the gallery container's margin.`,
  isRelevant: (styleParams) => true,
  type: INPUT_TYPES.NUMBER,
  default: 0,
}